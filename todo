提成PR的形式，方便评论


用import 不用include
leading trailing 不一定是左右，有本地化情况 比如 阿拉伯语

运行时 错误处理，不能到后续
error包装单独抽象出一个函数

todo 返回的数据不要用字典，用data model    所有的网络请求都转成data model
相关第三方库



两次点击搜索未禁用 由于天气请求未完成 异步

showalert完成放在主线程，完整的ui创建过程

ui组件的赋值也在主线程 418行 所有动画

天气信息提取要加类型判断

